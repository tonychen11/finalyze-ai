# Google Generative AI API Key
GENERATIVE_API_KEY=your_api_key_here

# Gemini Model Selection
# Options: gemini-2.5-flash, gemini-3-pro-preview
GEMINI_MODEL=gemini-3-pro-preview

# Prompt Source Configuration
# Options: "env" (use GEMINI_PROMPT env var) or "file" (use src/prompts/gemini-analysis.txt)
# Use "env" for production (keep prompt secret) or "file" for development/customization
PROMPT_SOURCE=file

# Gemini Prompt (only used if PROMPT_SOURCE=env)
# Store your AI prompt here as a secret instead of in git
# Example: GEMINI_PROMPT=You are a financial analyst. Analyze the CSV...